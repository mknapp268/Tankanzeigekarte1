
// Tank dimensions in cm (outer dimensions)
export const TANK_WIDTH = 188;
export const TANK_DEPTH = 72;
export const TANK_HEIGHT = 160;

// Max content per tank in liters (according to manufacturer)
export const MAX_CONTENT_PER_TANK = 1500;

// Warning threshold in liters (total)
export const WARN_LIMIT_LITERS = 350;
